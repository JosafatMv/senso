export const setColorCircle = (score) => {
	if (score === 1) return 'circle-red';
	if (score === 2) return 'circle-orange';
	if (score === 3) return 'circle-yellow';
	if (score === 4) return 'circle-green';
	if (score === 5) return 'circle-blue';
};
